Guide reuses the current deterministic Plan and returns a staged operation protocol.
execute follows the current stage instructions continuously and exactly. Entries Auto completes Check, Diff, P-23, and atomic Apply inside Stage. After applied, run only Verify and Guide again. On repair_required, fix only failed findings and automatically Stage the same complete batch again; do not ask the user to continue. On stopped, classify existing formal-write and Recovery evidence: auto-replan a proven zero-write closure, Resume a provable postimage, or Rollback an exact preimage when policy selects it; report and end only on an approval/external-action boundary or hard block. Run Header and Curation through the actual commands returned by their respective Guide stages.
prepare_and_review stops before Apply; observe reports facts only; blocked stops immediately; complete ends the loop.
commands in Guide JSON are bound to the absolute path of the current aoci binary. Windows commands use PowerShell's & call operator. Ordinary --json output can be captured directly and passed to ConvertFrom-Json.
