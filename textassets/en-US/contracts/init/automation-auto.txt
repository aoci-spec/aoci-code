auto follows the current Guide stage continuously and exactly. Entries Stage completes Check, Diff, P-23, and atomic Apply internally. After any applied write batch, follow the next commands returned by the current Guide. For Volumes, continue Maintain while remaining is nonzero; after the final remaining=0 batch, run Verify, aggregate Check, and Guide. On repair_required, fix only the failed Entries and automatically Stage the same complete batch again without asking the user to continue. A stopped write attempt is automatically replanned, resumed, or rolled back only when existing evidence proves that action; stop the user task on an approval/external-action boundary, unprovable recovery, third-party conflict, or another real safety failure
