Author exactly one complete Database Entry for each table candidate: table_name[tag]: F:... | R:... | A:... | S:.... Read the Root and Meta rules, the complete current Table Evidence, the existing Entry when present, and relevant migrations, repositories, services, APIs, configuration, and tests before writing. Current canonical Evidence overrides an old Entry for schema facts; current source and tests remain the authority for business semantics.

Choose the complete tag and every F/R/A/S field yourself. AOCI supplies identity, evidence, validation, impact, and governance only; it does not generate, prefill, rewrite, shorten, or repair semantics. F is one core proposition and never a column list. R contains only strong relationships needed for safe understanding or change, not every foreign key. A contains only stable identifiers, access boundaries, and contract surfaces, not every repository, API, CRUD method, column, or index. Admit an S statement only when it is not inferable from the table name, tag, F, R, A, or ordinary schema facts and its removal could cause an incorrect change to code, schema, transactions, concurrency, lifecycle, retention, or cross-table write order. Do not infer S from names such as deleted_at, status, tenant_id, or created_at.

One base table remains one Entry. Columns, keys, constraints, indexes, and partitions are evidence, not separate cognition objects. Do not copy a Schema dump into FRAS, add every foreign key to R, add every caller to A, invent symmetric relations, or infer a rename. Preserve an old high-entropy S constraint only when current evidence and code still support it. Rewrite the complete Entry even when only one schema fact changed. Respect the Meta dictionary, FRAS v2 limits, R/A item limits, and the importance-based S quota. If any target lacks sufficient business evidence, stop and report it; never guess. Submit every candidate in the returned batch together with its unchanged batch_id and candidate_id.
