交付完整正式Whole-Index；Volumes v1仓库可选all、project、meta、code或database scope。普通调用在正文可装入overview_delivery.chunk_tokens时一次交付，否则立即返回使用overview-chunk-receipt/v1的第1块。看到continuation_required=true后必须原样提交next_cursor并自动继续到completed=true；不得询问用户、开始业务任务、给出阶段性结论，或用scope、aoci_search、aoci_get_entries补齐缺块。Host截断、缺块、重复、乱序、cursor失败或Index/配置变化时停止本次认知链。正式Entry ordinal是唯一规范序列中的1-based位置：Header、注释、空行、Section/Overview/Chunk Marker、Receipt与Metadata均不计数，Chunk Receipt与Challenge答案消费同一序列。完整链结束后只正式提交一次host_delivery_confirmation和既有model-cognition-attestation/v1；Attestation必须原样回绑最终Challenge发布的当前index_sha256、entry_sequence_sha256与entry_count，旧身份无效。同一响应只允许一次纯Schema修正，语义不匹配即fail/uncertain，禁止读取Memory、源码、Spec、aoci.txt、历史会话或调用Search、Get Entries、Scope旁路补答。Attestation为partial或fail时禁止无保留地声称完整系统认知，但不禁止诚实回答或source-bound调查与工程。ordinal、对象身份、Tag或核心F不匹配属于严格证明失败，本身不能证明模型没有理解系统。Host可以设置cognition_state_version=cognition-state/v2，接收把delivery_verified、model_cognition_usable、strict_attestation_verified、governance_aligned与current_system_cognition_reliable分开的增量只读投影；省略该字段时保持精确Legacy响应形状。上下文压缩刷新若传输完整、Host确认且治理对齐，则即使Attestation为partial或fail也消费该generation，Host继续原source-bound任务而不再次自动Overview。首次失败还会阻断未绑定源码的Root/Meta、Migration、全局布局和其他系统决策。system_mastery_percent只自评系统框架——架构、职责、强关系、稳定外部契约以及高熵安全和维护约束——不表示完整源码或运行实况知识。默认只向用户展示一句把真实索引覆盖率与框架掌握度分开的简洁结果。check_only=true只返回紧凑checkpoint，不进入分块链。正式认知Dirty或Stale时仍可交付但不可靠；aoci_maintain仍是治理路径，未决Recovery或不一致snapshot失败关闭。正文只包含开始Marker、原始aoci.txt字节和结束Marker，不生成目录清单。Legacy无参数与Volumes scope行为保持兼容。不会修改正式索引或Baseline，但可能追加本地审计记录。
