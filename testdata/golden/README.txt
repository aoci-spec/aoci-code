本目录保存用户可观察文本和模型合同的派生兼容快照。

测试只读取并比较这些文件，绝不在首跑或失败时自动改写。文本资源本身仍以
textassets清单登记的zh-CN文件为唯一生产事实源；Golden只是漂移探测器，不被
生产代码加载。*.sha256只对真实生产加载、渲染或组合函数的最终字节求摘要，避免保存
第二份合同正文；全文Golden只用于CLI/Guide等多资源装配结果，JSON Golden只用于完整公开
协议结构。除本说明外，每个Golden都必须由测试直接消费，禁止保留孤立快照。
