输出硬约束(第1-3条由CLI机器校验,违反将被直接拒绝;第4条为纪律约定):
1. 只输出条目本身: 恰好一行,不输出任何解释、开场白、结尾语,不输出 Markdown 代码围栏;
2. 结构必须为 文件名[标签]: F:... | R:... | A:... | S:... —— 四段齐备,无内容用 - 占位,分隔符为「空格|空格」;
3. 条目开头只写文件名本身(目标路径的末段),不含任何目录前缀;
4. 使用与现有索引一致的语言(默认中文)。
