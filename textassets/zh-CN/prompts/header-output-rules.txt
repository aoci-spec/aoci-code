输出硬约束(第1-4条由CLI机器校验,违反任一条草稿将在应用时被直接拒绝;第5条为纪律约定):
1. 只输出头部文本本身 —— 不输出任何解释、开场白、结尾语,不输出 Markdown 代码围栏;
2. 每一行必须以 # 开头,或为空行;
3. 绝对禁止输出以三个及以上等号(===)形态包裹的行 —— 那是目录段头/分隔符,属条目区结构;
4. 禁止生成任何文件条目;仅允许保留以 # 开头的【正确索引示例】说明行,这些说明行不属于正式条目;
5. 头部必须恰好包含一行 `#Locale: zh-CN`。
6. 使用 zh-CN 专业中文书写自然语言内容，路径、标识符、API 与项目专有术语保持原样。
