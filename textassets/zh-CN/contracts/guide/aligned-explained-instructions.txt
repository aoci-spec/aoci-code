结束完整索引生成循环；不要为CurationExcludedMissing或非Pending SkippedMissing强行生成条目。
verify会保留RawMissing原始事实；CurationExcludedMissing与非Pending SkippedMissing不导致失败，只有ActionableMissing、PendingCurationMissing、Orphan、Stale或Unbaselined未解决时才返回非零。
将本次aligned结果作为checkpoint证据。Agent决定下一阶段是否需要仓库全貌；之后显式调用普通Overview时完整交付请求scope，check_only继续作为紧凑检查路径。
