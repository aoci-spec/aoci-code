读取当前AOCI会话级运行合同。新的Agent Run没有可靠合同，或合同已不在有效上下文、AOCI服务身份发生变化时调用；同一认知周期不要重复调用。可选module_path返回每次新鲜加载的完整派生project-module-cognition/v1投影；省略时保持普通Rules和默认全量aoci_overview流程。模块投影无状态，不存在记忆或重置行为。不会修改正式索引或Baseline，但可能追加本地审计记录。
