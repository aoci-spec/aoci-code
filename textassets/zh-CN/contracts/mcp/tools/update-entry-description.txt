通过既有Impact、Projected Validation、Diff、CAS、AtomicWrite、Baseline/Database Binding、Receipt、Ledger和Recovery管线正式写入机器签发的当前完整模型创作Entry批次。Volumes Code创作必须原样携带无参数aoci_maintain返回的code_batch_id、每项candidate_id、path与source_sha256：aoci_update_entry.code_batch_id必须等于code_plan.batch_id和每个返回的Code candidate.batch_id；authoring_batch.batch_identity是不同的跨domain封装身份，绝不能填入code_batch_id。Database候选原样携带顶层batch_id和每项candidate_id，不得把Database Volume或Table Evidence哈希复制到source_sha256。max_entries只限制本次请求和原子事务，不限制logical plan、Whole-Index或Managed Scope。entries必须等于当前机器签发批次的完整候选集合，Host不得自行截取Maintain输出。AOCI在写入前验证全部候选及Source/Evidence、Scope、Composite、关系、Tag、FRAS、R/S和投影Budget绑定，使Code最多写一次、Database最多写一次、Baseline/Binding最多写一次。若R引用后续尚未写入的Code目标，机器零写入签发包含所需关系闭包的替代批次；不得删除或机械改写模型创作的R。成功批次若remaining非零，不得宣称Whole-Index aligned，应基于新preimage重新调用Maintain继续。可修拒绝一次返回全部确定性Finding，包含candidate_index、canonical_object_identity、rule_code与safe_repair_action；repair_required只修retry_scope并重提同一当前完整批次。既有直接单条、Legacy和兼容Code source_sha256输入保持支持。Root和Meta仅为Guard。证据不足时不要猜写，不得因transport上限缩减Scope或提交机器批次的子集。
