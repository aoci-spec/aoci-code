# 构建产物
/build/
/dist/

# Go相关
*.test
*.out
coverage.txt

# 编辑器与操作系统
.DS_Store
.idea/
.vscode/

# 人工操作产生的时间戳备份文件
*.backup.*

# ── AOCI本地运行时产物：只属于当前机器，不进入Git ──

# 程序滚动备份
.aoci/*.bak

# 本地遥测台账：记录操作与Token，不包含密钥，但跨机器合并无意义
.aoci/ledger.jsonl

# Agent登记的不确定项与维护待办：只服务当前工作区会话
.aoci/reports.jsonl

# Verify历史快照
.aoci/verify_history/

# 本地覆盖配置：可包含个人模型端点等本机信息
.aoci/config.local.json

# Draft-first草稿区：正式Apply前不属于团队认知资产
.aoci/drafts/

# 跨进程写锁：仅在提交临界区短暂存在
.aoci/lock

# 可恢复长时任务：保存本机执行命令、检查输出与安全检查点
.aoci/tasks/

# 可恢复认知Onboarding：只保存本机确定性进度和正式Apply前工件
.aoci/onboarding/

# ── Agent宿主集成配置：由aoci init生成，只属于当前机器，不进入Git ──
#
# 这些文件的command/args来自NewTplData的BinPath与RepoRoot，两者都是绝对路径，
# 跨机器一律失效。且各安装判据只检查条目存在与否、不校验路径是否属于本机
# （IsClaudeMCPInstalled只看mcpServers.aoci这个key，IsClaudeHookInstalled只看
# command是否引用claude-pretool.sh），所以误提交后他人的init会幂等早退、静默
# 空转，配置永远修不好。.aoci/下的产物(含hooks/claude-pretool.sh)不在此列，
# 已由.aoci/.gitignore的默认拒绝边界覆盖。

.mcp.json
.claude/settings.json
.codex/config.toml
.cursor/mcp.json
opencode.json
opencode.jsonc
.opencode/

# ── AOCI正式认知与治理资产：必须进入Git ──
#
# aoci.txt、Baseline、团队配置和文件级Curation决策共同构成可克隆、
# 可审计的仓库认知状态。反忽略规则显式登记资产边界，防未来扩大
# .aoci忽略范围时误伤正式资产。

!aoci.txt
!.aoci/baseline.json
!.aoci/database-baseline.json
!.aoci/config.json
!.aoci/curation.json
