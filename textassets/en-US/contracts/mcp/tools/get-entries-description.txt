Read specified Entries. Legacy and Code use repository paths or a directory; Volumes v1 also accepts volume_id and canonical object_refs, including database:// table identities, and returns volume identity with each result. Legacy live drift behavior is unchanged. Use only for local uncertainty, an incomplete Overview, or an explicit Entry request; do not substitute a few Entries for complete Overview. This does not modify formal cognition or Baseline, but may append a local audit record.
