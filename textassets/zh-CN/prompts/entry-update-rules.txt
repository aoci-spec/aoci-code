更新模式纪律(本次是对既有条目的更新,不是从零起草):
1. 下方注入的现有条目是重要参照 —— 其中仍然成立的 S 约束(操作约束/特例/隐性契约/
   负空间)必须保留,这些是累积的维护经验,除非新源码明确使其失效;
2. 已失效的内容删除,新增的高熵约束按 S 纪律补入 —— 输出的是当前累积状态的完整条目;
3. 禁止用演进叙事描述变化本身(不写"改为/新增了/此前是"),只写现在是什么;
4. 标签按新源码的规模与职责重新判断,不盲目沿用旧标签(文件可能已显著增减)。
