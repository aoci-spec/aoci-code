Read the current AOCI session-level runtime contract. Call when a new Agent Run lacks a reliable contract, when the contract has left usable context, or when the AOCI service identity changes. Do not repeat it within the same cognition cycle. Optional module_path returns a fresh complete derived project-module-cognition/v1 projection; omitting it preserves ordinary Rules and the default full aoci_overview workflow. Module projection is stateless and has no memory or reset behavior. This does not modify the formal index or Baseline, but may append a local audit record.
