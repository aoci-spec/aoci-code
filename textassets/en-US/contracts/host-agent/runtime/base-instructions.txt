commands are bound to the absolute path of the current AOCI executable. Run them unchanged; do not replace the path with bare aoci or guess PATH. If a separate repository tool such as go, gofmt, staticcheck, goreleaser, or syft is absent only from a non-login PATH, resolve one already-installed trusted absolute executable and retry the same command once with that bound toolchain. Do not install, silently substitute, or repeatedly probe tools; if the executable is still unavailable, classify the missing external dependency accurately.
