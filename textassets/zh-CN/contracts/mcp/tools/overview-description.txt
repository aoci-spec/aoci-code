交付完整正式Whole-Index；Volumes v1仓库可选all、project、meta、code或database scope。overview_delivery.chunk_tokens默认600000；正文可容纳时一次交付。成功结果的模型可见content只含开始Marker、原始aoci.txt字节和结束Marker，cursor、哈希、Receipt、状态与Challenge等传输字段只在Host可见的顶层_meta。单响应到达BODY_END即完成，模型不提交host_delivery_confirmation或model-cognition-attestation/v1，也不为AOCI生成额外回复，直接继续原任务。仅当显式小阈值产生continuation_required=true时，Host从_meta读取next_cursor并自动继续到completed=true；不得让模型逐块决策、询问用户、开始业务任务、给出阶段性结论，或用scope、aoci_search、aoci_get_entries补齐缺块。兼容多块链仍使用overview-chunk-receipt/v1，并可在末块后使用既有host_delivery_confirmation与model-cognition-attestation/v1；其Attestation仍须回绑当前index_sha256、entry_sequence_sha256与entry_count。Host截断、缺块、重复、乱序、cursor失败或Index/配置变化时失败关闭。Host可以设置cognition_state_version=cognition-state/v2，接收分离的delivery_verified、model_cognition_usable、strict_attestation_verified、governance_aligned与current_system_cognition_reliable只读投影。check_only=true只返回紧凑checkpoint；Dirty或Stale认知仍可交付但不可靠，aoci_maintain仍是治理路径，Recovery或不一致snapshot失败关闭。不生成目录清单，不会修改正式索引或Baseline，但可能追加本地审计记录。
