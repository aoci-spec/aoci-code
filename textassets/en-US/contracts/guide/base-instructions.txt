Follow the order and stop conditions in the current instructions exactly. Run guide again only when instructions explicitly require it. Until then, preserve the bindings to the current plan_id, every request source_sha256, and the run_id returned by this stage.
