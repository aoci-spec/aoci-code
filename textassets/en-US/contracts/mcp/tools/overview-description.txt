Deliver the complete formal Whole-Index, optionally scoped to all, project, meta, code, or database in a Volumes v1 repository. overview_delivery.chunk_tokens defaults to 600000, so a body that fits is delivered once. For a successful delivery, model-visible content contains only the start marker, exact aoci.txt bytes, and end marker; cursor, hashes, Receipt, state, Challenge, and other transport fields are available only to the Host in top-level _meta. A single response that reaches BODY_END is complete: the model does not submit host_delivery_confirmation or model-cognition-attestation/v1 and does not generate an extra AOCI reply before continuing the task. Only when an explicitly smaller budget produces continuation_required=true does the Host read next_cursor from _meta and continue automatically until completed=true; the model must not decide Chunk calls, ask the user, begin the task, state a partial conclusion, or replace missing Chunks with scope, aoci_search, or aoci_get_entries. The compatible multi-Chunk path still uses overview-chunk-receipt/v1 and may use the existing host_delivery_confirmation and model-cognition-attestation/v1 after the final Chunk; that Attestation remains bound to the current index_sha256, entry_sequence_sha256, and entry_count. Host truncation, missing, duplicate, or reordered Chunks, cursor failure, or Index/configuration change fails closed. A Host may request cognition_state_version=cognition-state/v2 to receive the separate model_cognition_usable, strict_attestation_verified, governance_aligned, and current_system_cognition_reliable projection. check_only=true returns a compact checkpoint; Dirty or Stale cognition remains unreliable, aoci_maintain remains the governance path, and Recovery or an incoherent snapshot fails closed. No directory inventory is generated. This does not modify formal cognition or Baseline, but may append a local audit record.
